using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BlackBorad_Color : MonoBehaviour
{
    private Color c;
    // Start is called before the first frame update
    void Start()
    {
        c = new Color();


    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
